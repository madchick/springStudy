spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/testdb
    username: testdb
    password: testdb
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true